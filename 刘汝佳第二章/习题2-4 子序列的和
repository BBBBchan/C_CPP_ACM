#include <stdio.h>
int mian ()
{
	double n , m ;
	while(scanf("%lf %lf", &n ,&m) == 2 && n < m && n != 0 )
	{
		int s = 0;
		for(int i = n ; i < m ;i++)
		{
			s += 1 / (i * i);
		}
		printf("%.5f", s);
	}
	return 0 ;
}