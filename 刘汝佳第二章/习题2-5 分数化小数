#include <stdio.h>
int main()
{
	int a, b, c, kase = 0;
	while(scanf("%d %d %c", &a, &b, &c) == 3 && (a || b || c)){
		double x = 0;
		x = (double) a / b ;
		printf("Caes %d: %.cf",++kase, x);
	} 
	return 0 ;
}