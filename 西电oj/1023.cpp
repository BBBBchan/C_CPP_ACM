#include <stdio.h>
struct data{
	int x;
	int y;
	int id;
}a[n];
int main(){
	int T;
	scanf("%d", &T);
	while(T--){
		int n;
		scanf("%d", &n);
		for(int i = 0; i < n; i++)
			scanf("%d", &a[i].x, &a[i].y, &a[i].id);
		int m;
		scanf("%d", &m);
		int b[m];
		for(int i = 0; i < m; i++)
			scanf("%d", &b[i]);
		for(int i = 0; i < m; i++){
			if()
		}
	}
}